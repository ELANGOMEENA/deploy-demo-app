apiVersion: v1
kind: Service
metadata:
  name: demp-python-app-service
  labels:
    app: demo-python-app
    namespace: default
spec:
  selector:
    app: demo-python-app
  ports:
    - protocol: TCP
      port: 5000
      targetport: 5000
  type: ClusterIP

apiVersion: v1
kind: Service
metadata:
  name: demp-pyapp-service
  namespace: default
  labels:
    app: demo-pyapp
spec:
  selector:
    app: demo-pyapp
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 5000
  type: ClusterIP
